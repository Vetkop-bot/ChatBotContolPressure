# Бот контроля артериального давления

Telegram-бот для ввода показаний давления (систола, диастола, пульс).  
На данный момент работает в режиме распознавания чисел из текста и фото, сохранение истории в PostgreSQL. 

## Возможности (текущие)

- Приём сообщений с тремя числами (разделители: пробел, запятая, точка с запятой, слеш, двоеточие)
- Распознавание систолы, диастолы, пульса
- Ответ с распознанными значениями
- Распознавание по фото 
- Сохранение в БД 	
- Логирование сообщений в консоль сервера
- Команды 
  - `/start` – приветствие и главное меню с кнопками.
  - `/help` – справка.
  - `/stats` – последние 5 замеров с индексами.
  - `/settings` – просмотр настроек.
  - `/set_medication <название>` – сохранить препарат.
  - `/set_norm <систола> <диастола>` – установить личную норму.
  - `/set_notifications on|off` – включить/отключить уведомления о превышении.
- **Интерактивные кнопки** – в Telegram доступны кнопки для быстрого доступа к статистике, настройкам и помощи.

## Технологии

- **Node.js**
- **TypeScript**
- **Telegraf** – фреймворк для Telegram Bot API
- **Express** – для приёма вебхуков MAX
- **Prisma** – ORM для работы с PostgreSQL
- **PostgreSQL** – база данных (запускается в Docker)
- **YOLOv8s (Ultralytics)** – архитектура детекции объектов
- **ONNX Runtime** – высокопроизводительный инференс модели в Node.js
- **Sharp** – обработка изображений (обрезка, изменение размера, контраст)
- **Docker** – для запуска PostgreSQL

## Установка и запуск

### Клонируй репозиторий
git clone https://github.com/Vetkop-bot/ChatBotContolPressure.git
cd ChatBotContolPressure
### Установи зависимости
npm install
### Создай файл .env в корне проекта
BOT_TOKEN= 
DATABASE_URL=
MAX_BOT_TOKEN=
MAX_WEBHOOK_SECRET
### Запуск PostgreSQL через Docker
docker compose up -d
### Применение мишрацций Prisma
npx prisma generate
npx prisma migrate dev --name init
### Запусти ботов
npm run dev:telegram
npm run dev:max
Оба одновременно
npm run dev